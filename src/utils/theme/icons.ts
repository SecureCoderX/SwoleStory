import { Dumbbell, FileClock, History, User, Star, PlusCircle, XCircle, HeartPulse } from 'lucide-react-native';

const icons = {
  workout: Dumbbell,
  cardio: HeartPulse,
  log: FileClock,
  history: History,
  profile: User,
  achievements: Star,
  add: PlusCircle,
  close: XCircle,
};

export default icons;
